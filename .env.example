# Server
PORT=8080
CLIENT_URL=http://localhost:3000
NODE_ENV=development
SESSION_SECRET=change_me

# MongoDB (opcional - actualmente deshabilitado en el código)
DB_NAME=
DB_USER=
DB_PASS=

# OAuth GitHub (no utilizado en esta API, reservado para futuro)
CLIENT_ID=
CLIENT_SECRET=
CALLBACK_URL=

# Nodemailer (no utilizado en esta API, reservado para futuro)
NM_SERVICE=
NM_PORT=
NM_USER=
NM_PASSWORD=

# JWT (no utilizado en esta API, reservado para futuro)
JWT_COOKIE_NAME=
JWT_SECRET=
